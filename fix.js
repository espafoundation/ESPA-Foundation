const fs = require('fs');
let code = fs.readFileSync('src/components/Modals.tsx', 'utf-8');

// The original file is too mangled, I should find a pristine version or just rewrite the Modals.tsx if I can find the original text.
